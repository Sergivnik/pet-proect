import React, { useEffect, useMemo, useState } from 'react';
import { useDispatch } from 'react-redux';
import './stocks.sass';

export const AddStock = () => {
  return <div className="addStockContainer"></div>;
};
